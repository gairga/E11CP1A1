<h1>Pages#one</h1>
<p>Pagina UNO</p>

<%= link_to "Pagina DOS", pages_two_path %>

<% 2+2 %>


<div class="container">
  <div class="row">
    <div class="col-sm">
      One of three columns
    </div>
    <div class="col-sm">
      One of three columns
    </div>
    <div class="col-sm">
      <% @products.each do |p| %>
          <h1><%= p %></h1>
      <% end %>
    </div>
  </div>
</div>
